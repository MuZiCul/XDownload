# Use hoisted (flat) node_modules without junction links.
# Fixes: optional-dependency junction links break under Node 22 (e.g.
# @tauri-apps/cli-win32-x64-msvc, @esbuild/win32-x64 resolve to MODULE_NOT_FOUND).
node-linker=hoisted
